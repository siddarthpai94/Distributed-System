# Lab 08 — Quorum Reads and Writes

Implement leaderless replication with configurable `n`, `w`, `r`. Return latest version on reads and implement read-repair.

Test quorum availability and stale reads.

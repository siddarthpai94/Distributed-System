# Lab 05 — Read Consistency Guarantees

Implement read modes: `read-after-write`, `monotonic-read`, `consistent-prefix`, and default `eventual`.

Approaches: route reads to leader, pin client sessions, or track causal metadata.

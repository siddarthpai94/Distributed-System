# Lab 03 — Synchronous vs. Asynchronous Replication

Implement `SYNC`, `ASYNC`, and `SEMI_SYNC` replication modes and measure write latency under each.

Tip: simulate a slow follower by injecting artificial sleep before applying entries.
